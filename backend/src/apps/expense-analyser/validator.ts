import { ValidationError } from "../todo/validator.js"

export { ValidationError }

export const expenseAnalyserValidator = {
  // File validation is handled by multer middleware
  // This validator can be extended for other validations if needed
}
